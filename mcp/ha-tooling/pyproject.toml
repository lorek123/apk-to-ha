# SPDX-License-Identifier: MIT
[project]
name = "ha-tooling-mcp"
version = "0.1.0"
description = "F-0a — MCP server wrapping ruff, mypy, hassfest, pytest for HA integration validation."
requires-python = ">=3.12"
dependencies = [
    "mcp[cli]>=1.0",
    "fastmcp>=0.1",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
