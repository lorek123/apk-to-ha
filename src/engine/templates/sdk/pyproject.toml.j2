# SPDX-License-Identifier: MIT
[project]
name = "{{ sdk_package }}"
version = "{{ integration_version }}"
description = "Auto-generated SDK for {{ name }}"
requires-python = ">=3.12"
dependencies = ["aiohttp>=3.10"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
